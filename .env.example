# Slack app credentials (multi-workspace OAuth)
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=

# Shared key for LOCAL DEV ONLY. In production each workspace registers its own key via /setup-rd.
REALITY_DEFENDER_API_KEY=
ALLOW_SHARED_RD_KEY=false

LOG_LEVEL=INFO
PORT=3000
